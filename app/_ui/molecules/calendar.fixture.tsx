import { Calendar } from "$/_ui/molecules/calendar";
import React from "react";

export const Fixture = {
  basic: () => <Calendar.Basic year={2023} month={7} day={14} />,
};

export default Fixture;
